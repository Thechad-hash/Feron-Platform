{
  "name": "feron-platform",
  "private": true,
  "workspaces": [
    "feron-ethereum",
    "trial-license-ui",
    "smart-contracts-ui",
    "feron-live-client",
    "feron-live-server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm --workspace trial-license-ui run dev\" \"npm --workspace smart-contracts-ui run dev\" \"npm --workspace feron-live-client run dev\" \"npm --workspace feron-live-server run start\"",
    "build": "npm --workspace trial-license-ui run build && npm --workspace smart-contracts-ui run build && npm --workspace feron-live-client run build",
    "deploy:contracts": "npm --workspace feron-ethereum run deploy",
    "lint:css": "tailwindcss --check ./trial-license-ui/src && tailwindcss --check ./smart-contracts-ui/src && tailwindcss --check ./feron-live-client/src"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.4",
    "autoprefixer": "^10.4.21",
    "concurrently": "^8.0.1",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.2.2",
    "vite": "^4.5.0"
  }
}
